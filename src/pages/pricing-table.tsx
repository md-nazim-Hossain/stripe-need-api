import React from 'react';

import PricingTable from '@components/Stripe/StripePricingTable/PricingTable';

type Props = {};

const handler = (props: Props) => {
  return <PricingTable />;
};

export default handler;
